# TODO
CC = gcc
CFLAGS = -Wall -Werror -Wextra -pedantic -std=c99 -I -02 -DNDBUG

DIRS = $(dir $(wildcard src/*/))
SRC = $(DIRS)$(DIRS).c
OBJ = $(DIRS)$(DIRS).o

all : ${OBJ}

.PHONY: clean
clean :
	${RM} ${OBJ}